[pytest]
required_plugins = pytest-asyncio
pythonpath = .
addopts = -v
asyncio_mode = auto
markers =
    e2e: testes fim-a-fim contra um endpoint real (requer E2E_BASE_URL)
    integration: testes de integração que dependem de serviços (Redis/DB/RabbitMQ)